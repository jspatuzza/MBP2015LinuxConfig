# Ver plymouth-poweroff.service.d/override.conf — mismo helper idempotente
# para el camino halt (equivalente a shutdown).
[Service]
Type=oneshot
ExecStartPre=
ExecStart=
ExecStart=/usr/local/bin/plymouth-shutdown-start shutdown
ExecStartPost=
