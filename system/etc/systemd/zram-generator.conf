# zram como swap primario — MBP12,1 8GB (auditoría RAM 2026-07-05)
# zstd ~3:1; prio 100 > /swap.img (-2): el disco DRAM-less queda solo como desborde.
[zram0]
zram-size = min(ram / 2, 4096)
compression-algorithm = zstd
swap-priority = 100
