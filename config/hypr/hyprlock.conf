# /* ---- 💫 https://github.com/JaKooLit 💫 ---- */  #
# Hyprlock config for => 2k monitor resolutions
# Original config submitted by https://github.com/SherLock707

# Sourcing colors generated by wallust
source = $HOME/.config/hypr/wallust/wallust-hyprland.conf
# Color de fondo y texto según modo claro/oscuro (actualizado por DarkLight.sh)
source = $HOME/.config/hypr/hyprlock-theme.conf
$Scripts = $HOME/.config/hypr/scripts

general {
    fractional_scaling = 2
    immediate_render = true
}

background {
    monitor =
    path =
    color = $hyprlock_bg
}

# LABEL
label {
    monitor =
    text = Contraseña
    color = $hyprlock_text
    font_size = 20
    font_family = Ubuntu Sans Light
    position = 0, 45
    halign = center
    valign = center
}

# INPUT FIELD
input-field {
    monitor =
    size = 330, 50
    rounding = 6
    outline_thickness = 2
    dots_size = 0.33
    dots_spacing = 0.4
    dots_center = true
    dots_rounding = -1
    outer_color = $hyprlock_accent
    inner_color = $hyprlock_inner
    capslock_color = rgb(255,255,255)
    font_color = $hyprlock_text
    fade_on_empty = false
    font_family = Ubuntu Sans
    placeholder_text =
    hide_input = false
    position = 0, -10
    halign = center
    valign = center
}
